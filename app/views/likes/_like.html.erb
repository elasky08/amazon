<% if user_like.present? %>
  <%= link_to "Un-like", product_review_like_path(@product, review, user_like), method: :delete, remote: true  %>
<% else %>
  <%= link_to "like", product_review_likes_path(@product, review), method: :post, remote: true  %>
<% end %>
